{
  "name": "add4snorgnotev3-clipper",
  "version": "0.1.0",
  "private": true,
  "description": "Snorgnote browser clipper extension (direct deep-link payload).",
  "scripts": {
    "test": "node --test tests/**/*.test.js"
  }
}
