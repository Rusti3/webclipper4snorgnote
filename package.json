{
  "name": "add4snorgnotev3-clipper",
  "version": "0.1.0",
  "private": true,
  "description": "Snorgnote browser clipper extension and local helper server.",
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "helper:start": "node src/helper/server.js"
  }
}
